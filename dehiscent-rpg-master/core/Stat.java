package core;

/**
 * The different statistics a player can have.
 * Most of them are set, levelled and modified,
 * phys_def is solely calculated with equipment
 * modifiers.
 */
public enum Stat {
  VIT, STR, DEX, INT, PHYS_DEF
}
