package org.mysoft.td.test;

import org.mysoft.td.engine.objects.GenericTurrent;
import org.mysoft.td.engine.objects.sample.MachineGunTurrent;
import org.mysoft.td.engine.world.sample.SampleWorld;

public class Test2 {

	public static void main(String[] args) {
		
		SampleWorld world = new SampleWorld();
		
		GenericTurrent t1 = new MachineGunTurrent(world);
		
		t1.setPosition(10, 10);
		
		//PathCalculator pc = new PathCalculator(world);
		
		//StatefulPath path = pc.calculatePath(IntPoint.create(0, world.getHeight() / 2), IntPoint.create(world.getWidth() - 1, world.getHeight() / 2));
		
		/*
		for(Iterator it = path.getPoints().iterator() ; it.hasNext();) {
			IntPoint p = (IntPoint)it.next();
			System.out.println("(" + p.x + ", " + p.y + ")");
		}
		*/
	}
	
}
